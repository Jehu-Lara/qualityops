# Power BI

The planned deliverable is one **Process Health** page after Python results have been validated against Minitab. The `.pbix` file is ignored because it can embed source data, credentials, queries, and internal paths. Publish only a sanitized template or static preview after review.
