# Notebooks

Use notebooks only for exploration. Reusable calculations belong in `src/qualityops/` and must be covered by deterministic tests. Clear cell outputs before committing if they could disclose source data.
